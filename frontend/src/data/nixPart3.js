// Learn Nix course content — Part 3 (Modules 6–10 + reference material)

export const nixPart3 = [
  {
    id: "callpackage",
    title: "callPackage & Overriding Packages",
    group: "Module 6 — Advanced Packaging & Nixpkgs Idioms",
    summary: "The dependency-injection pattern at the heart of nixpkgs.",
    content: [
      { t: "h", text: "The callPackage pattern" },
      { t: "p", text: "nixpkgs packages are functions: { stdenv, fetchFromGitHub, cmake, … }: stdenv.mkDerivation { … }. callPackage reads the argument names and auto-injects matching packages from pkgs — dependency injection without any wiring." },
      { t: "read", code: "# package.nix\n{ stdenv, fetchFromGitHub, cmake, pkg-config, openssl }:\n\nstdenv.mkDerivation {\n  pname = \"my-app\";\n  version = \"1.0\";\n  src = fetchFromGitHub { ... };\n  nativeBuildInputs = [ cmake pkg-config ];\n  buildInputs = [ openssl ];\n}\n\n# Somewhere else: callPackage reads the args and injects them from pkgs\npkgs.callPackage ./package.nix {}\n# Any argument NOT in pkgs can be supplied in the second {} argument" },
      { t: "note", text: "This is why nixpkgs is so consistent: every package declares exactly what it needs, and the framework wires it. The same function can be called with different arguments to create variants — which brings us to overriding." },
      { t: "h", text: "override vs overrideAttrs" },
      { t: "read", code: "# override — change the function's ARGUMENTS (before evaluation)\npkgs.hello.override { withPerl = false; }\n\n# overrideAttrs — change the derivation's ATTRIBUTES (after evaluation)\npkgs.hello.overrideAttrs (oldAttrs: {\n  pname = \"hello-custom\";\n  buildInputs = oldAttrs.buildInputs ++ [ pkgs.curl ];\n  patches = oldAttrs.patches or [] ++ [ ./my-patch.patch ];\n})\n\n# Common pattern: a variant of a package\nmyVim = pkgs.vim.overrideAttrs (old: {\n  buildInputs = old.buildInputs or [] ++ [ pkgs.python3 ];\n})" },
      { t: "p", text: "A rule of thumb: override for options the package author exposed (function arguments); overrideAttrs for anything else, using the old attributes to build on top of the original." },
    ],
    exercises: [],
  },

  {
    id: "overlays",
    title: "Overlays & Setup Hooks",
    group: "Module 6 — Advanced Packaging & Nixpkgs Idioms",
    summary: "Globally modifying the package set, and how packages extend each other.",
    content: [
      { t: "h", text: "Overlays — global package modifications" },
      { t: "p", text: "An overlay is a function final: prev: { … } that creates a modified view of nixpkgs. Everything built through that package set sees the changes — this is how you patch packages without forking nixpkgs." },
      { t: "read", code: "# ~/.config/nixpkgs/overlays/my-overlay.nix\nfinal: prev: {\n  # Add a new package\n  my-hello = final.callPackage ./my-hello.nix {};\n\n  # Override an existing package globally\n  vim = prev.vim.overrideAttrs (old: {\n    # customization...\n  });\n\n  # Patch all Python packages (advanced)\n  python3 = prev.python3.override {\n    packageOverrides = pyFinal: pyPrev: {\n      requests = pyPrev.requests.overrideAttrs (old: {\n        patches = old.patches or [] ++ [ ./requests-fix.patch ];\n      });\n    };\n  };\n}" },
      { t: "read", code: "# Apply an overlay:\n# Via NixOS:      nixpkgs.overlays = [ (import ./my-overlay.nix) ];\n# Via flake:      overlays.default = final: prev: { ... };" },
      { t: "note", text: "final and prev are the two package sets: final is the modified one (referencing it gives you the overlay's own changes), prev is the original. Use prev to avoid infinite recursion, final to compose with other overlays." },
      { t: "h", text: "Build hooks & setup hooks" },
      { t: "p", text: "mkDerivation gives you phase hooks (preConfigure, postInstall, …) to run commands at precise points. Setup hooks are subtler: a package can ship a script that is sourced in the environment of any package that depends on it — how pkg-config automatically adds its PKG_CONFIG_PATH." },
      { t: "read", code: "preConfigure = \"echo 'About to configure'\";\npostInstall = \"echo 'Done installing'\";\n\n# A setup hook is a script that sets up the environment for dependents.\n# Example: pkg-config's setup hook adds PKG_CONFIG_PATH automatically.\n# You can write custom setup hooks to propagate env vars, flags, etc." },
    ],
    exercises: [],
  },

  {
    id: "language-builders",
    title: "Language-Specific Builders & Cross-Compilation",
    group: "Module 6 — Advanced Packaging & Nixpkgs Idioms",
    summary: "Beyond mkDerivation: purpose-built builders for each ecosystem.",
    content: [
      { t: "p", text: "While stdenv.mkDerivation works for anything, each language ecosystem has specialized builders that handle its dependency fetching, build tooling and test conventions." },
      { t: "table", head: ["Language", "Builder", "Notes"], rows: [
        ["Python", "python3Packages.buildPythonApplication / buildPythonPackage", "Handles pip, setuptools, wheels"],
        ["Node.js", "buildNpmPackage, buildYarnPackage", "Fetches deps via npmDepsHash / yarnNix"],
        ["Rust", "buildRustPackage", "Uses cargoSha256 for vendored deps"],
        ["Go", "buildGoModule", "Uses vendorHash for Go modules"],
        ["Haskell", "haskellPackages.callCabal2nix", "Cabal-based builds"],
        ["Emacs", "emacsPackages.trivialBuild", "Emacs Lisp packages"],
      ]},
      { t: "h", text: "Example — Rust" },
      { t: "read", code: "{ rustPlatform, fetchFromGitHub }:\nrustPlatform.buildRustPackage {\n  pname = \"my-rust-cli\";\n  version = \"0.1.0\";\n  src = fetchFromGitHub { ... };\n  cargoSha256 = \"sha256-AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=\";\n  # Or use cargoLock.lockFile = ./Cargo.lock; for workspace projects\n}" },
      { t: "h", text: "Example — Python" },
      { t: "read", code: "{ buildPythonPackage, fetchPypi, requests, pytest }:\nbuildPythonPackage rec {\n  pname = \"my-python-pkg\";\n  version = \"0.1\";\n  src = fetchPypi {\n    inherit pname version;\n    sha256 = \"...\";\n  };\n  propagatedBuildInputs = [ requests ];\n  nativeCheckInputs = [ pytest ];\n}" },
      { t: "h", text: "Cross-compilation" },
      { t: "p", text: "Nix can build for a different architecture than the one you're on. pkgsCross provides ready-made cross toolchains." },
      { t: "read", code: "nix build .#packages.aarch64-linux.my-app\n\n# In a flake:\npackages = {\n  default = pkgs.callPackage ./package.nix {};\n  for-arm = pkgs.pkgsCross.aarch64-multiplatform.callPackage ./package.nix {};\n};" },
      { t: "h", text: "Exercises — try on your machine" },
      { t: "list", items: [
        "Write a callPackage-compatible package for a small C program.",
        "Use overrideAttrs to create a variant of an existing nixpkgs package.",
        "Create an overlay that customises neovim globally.",
        "Package a Rust CLI tool with buildRustPackage.",
        "Package a Python library with buildPythonPackage and add a test dependency.",
      ]},
    ],
    exercises: [],
  },

  {
    id: "fhs-environments",
    title: "FHS Environments: Running Binary-Only Software",
    group: "Module 6 — Advanced Packaging & Nixpkgs Idioms",
    summary: "buildFHSEnv & buildFHSEnvBubblewrap — a synthetic /usr for software that assumes a standard Linux layout.",
    content: [
      { t: "h", text: "The problem: Nix's filesystem isn't the FHS" },
      { t: "p", text: "Nix keeps everything in /nix/store and puts symlinks on your PATH. There is no real /usr/bin, /lib or /etc. Most software never notices — Nix patches rpaths and wraps binaries. But some software assumes the standard Linux Filesystem Hierarchy Standard (FHS): npm packages that ship prebuilt native binaries, installers, and tools that hardcode paths like /usr/lib or exec /usr/bin/env. On NixOS these fail with baffling errors, because the directories they expect simply don't exist." },
      { t: "h", text: "buildFHSEnv & friends" },
      { t: "p", text: "buildFHSEnv builds a derivation whose output is a wrapper script that launches a synthetic FHS: a real /usr, /lib and /etc populated from the packages you declare. Programs inside it see the filesystem layout they were written for. Current nixpkgs provides two implementations plus an alias: buildFHSEnvBubblewrap uses bubblewrap with user namespaces and needs no root (the modern default); buildFHSEnvChroot uses chroot and needs root; buildFHSEnv is an alias for the bubblewrap implementation, and buildFHSUserEnv is the name you'll see in older code." },
      { t: "read", code: "pkgs.buildFHSEnvBubblewrap {\n  name = \"my-fhs-env\";\n\n  # Packages appear inside the env — note the p: function\n  targetPkgs = p: with p; [ git nodejs_22 cacert ];\n\n  # Shell snippet sourced on entry (env vars, aliases…)\n  profile = ''\n    export FOO=bar\n  '';\n\n  # The command run on entry (defaults to bash)\n  runScript = \"bash\";\n}" },
      { t: "p", text: "targetPkgs receives the environment's package set and returns the packages to expose — they land in /usr/bin, /lib, etc. multiPkgs is the same idea for multiple architectures (e.g. 32-bit libraries), which is how nixpkgs runs Steam. runScript is what executes when you launch the environment, and profile is sourced first so you can set variables." },
      { t: "h", text: "Under the hood: a symlink farm inside a bwrap namespace" },
      { t: "p", text: "Nothing is copied into the environment. Its real output is a wrapper script plus a 'symlink farm' — /usr/bin/git is just a symlink into /nix/store. At runtime the wrapper asks bubblewrap (binary: bwrap — the command you see below) to mount that farm as a real /usr, with /nix/store bound in read-only behind it, then execs your runScript inside the private namespace." },
      { t: "read", code: "# The derivation's real output: a wrapper script plus a 'symlink farm':\n#   /nix/store/<hash>-my-env/bin/my-env      — the wrapper you run\n#   /nix/store/<hash>-my-env/usr/bin/git  -> /nix/store/…-git-2.4x/bin/git\n\n# Running the wrapper builds a private namespace with bubblewrap (simplified):\n#   - /nix/store is bound in read-only, so every symlink target resolves\n#   - the farm is mounted as a real /usr; /bin, /sbin, /lib, /lib64 alias it\nbwrap_args=(\n  --unshare-all\n  --ro-bind /nix/store /nix/store\n  --ro-bind /nix/store/<hash>-my-env/usr /usr\n  --symlink usr/bin /bin\n  --symlink usr/sbin /sbin\n  --symlink usr/lib /lib\n  --symlink usr/lib64 /lib64\n  --proc /proc\n  --dev /dev\n  --tmpfs /tmp\n  --chdir /root\n)\nbwrap \"${bwrap_args[@]}\" bash -c 'source /etc/profile; exec runScript'\n\n# buildFHSEnvChroot does the same job with a real chroot(2) — hence the root requirement." },
      { t: "h", text: "Exposing it as a flake output" },
      { t: "p", text: "The natural home for an FHS environment is packages.default: nix run then enters it directly, and on NixOS you can add it to environment.systemPackages so the wrapped command lands on every user's PATH." },
      { t: "read", code: "{\n  inputs.nixpkgs.url = \"github:NixOS/nixpkgs/nixos-unstable\";\n\n  outputs = { self, nixpkgs }:\n    let\n      system = \"x86_64-linux\";\n      pkgs = nixpkgs.legacyPackages.${system};\n    in {\n      packages.${system}.default = pkgs.buildFHSEnvBubblewrap {\n        name = \"my-env\";\n        targetPkgs = p: with p; [ git nodejs_22 ];\n        runScript = \"bash\";\n      };\n    };\n}" },
      { t: "read", code: "nix run .    # enters the FHS shell\n# On NixOS, alternatively:  environment.systemPackages = [ self.packages.${system}.default ];" },
      { t: "note", text: "FHS environments are Linux-only: bubblewrap needs Linux user namespaces, and the whole point is a Linux /usr. On macOS the filesystem already resembles the FHS, so this idiom is a NixOS/Linux concern." },
      { t: "h", text: "In the wild: the Freebuff flake" },
      { t: "p", text: "freebuff (an npm CLI for agentic AI work) ships prebuilt native binaries that expect FHS paths, so plain npx fails on NixOS. github:jawuku/freebuff-nix wraps it in an FHS environment — a small, real-world flake worth studying end to end:" },
      { t: "read", code: "# flake.nix — github:jawuku/freebuff-nix (abridged)\n{\n  description = \"FHS-compliant shell for running the freebuff CLI via npx\";\n  inputs.nixpkgs.url = \"github:NixOS/nixpkgs/nixos-unstable\";\n\n  # Binary cache for the prebuilt FHS environment (pushed by CI)\n  nixConfig = {\n    extra-substituters = [ \"https://freebuff-nix.cachix.org\" ];\n    # Abridged — copy the full key verbatim from the real repo\n    extra-trusted-public-keys = [ \"freebuff-nix.cachix.org-1:…\" ];\n  };\n\n  outputs = { self, nixpkgs }:\n    let\n      freebuffEnv = system:\n        let pkgs = nixpkgs.legacyPackages.${system}; in\n        pkgs.buildFHSEnvBubblewrap {\n          name = \"freebuff\";\n          targetPkgs = p: with p; [ nodejs cacert yarn ];\n          profile = ''\n            export SSL_CERT_FILE=\"${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt\"\n            export NODE_EXTRA_CA_CERTS=\"${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt\"\n          '';\n          # --yes skips npm's interactive prompt; args after -- are forwarded\n          runScript = \"npx --yes freebuff\";\n        };\n    in {\n      packages = nixpkgs.lib.genAttrs [ \"x86_64-linux\" \"aarch64-linux\" ]\n        (system: { default = freebuffEnv system; });\n      apps = nixpkgs.lib.genAttrs [ \"x86_64-linux\" \"aarch64-linux\" ]\n        (system: {\n          default = { type = \"app\"; program = \"${self.packages.${system}.default}/bin/freebuff\"; };\n        });\n    };\n}" },
      { t: "read", code: "nix run github:jawuku/freebuff-nix                       # start a session\nnix run github:jawuku/freebuff-nix -- --continue 2026-08-01T15-51-31.852Z\n#                                  ^^ anything after -- reaches the CLI inside the FHS env" },
      { t: "p", text: "Three details worth noticing: the environment is a first-class package, so anyone can nix run it without installing anything; the nixConfig block points at a Cachix binary cache, so the (sizeable) FHS closure is downloaded prebuilt instead of built locally; and runScript receives the arguments after --, which is how a wrapper becomes a transparent CLI." },
      { t: "h", text: "When to reach for an FHS environment" },
      { t: "list", items: [
        "npm/yarn CLIs that ship prebuilt native binaries (esbuild, sharp, some agent CLIs).",
        "Closed-source or vendored binaries that hardcode /usr paths or exec /usr/bin/env.",
        "Installers and tools that expect a real /etc or /lib layout.",
        "Prefer real packaging (buildNpmPackage, buildRustPackage, mkDerivation) whenever the software can be built properly — FHS environments trade purity for compatibility and are the fallback, not the default.",
      ]},
      { t: "h", text: "Exercises — try on your machine" },
      { t: "list", items: [
        "Create a flake whose packages.default is a buildFHSEnvBubblewrap with nodejs_22 and git; nix run . and verify ls /usr/bin shows them (Linux only — on macOS, follow along on a Linux box/VM).",
        "Find an npm CLI that fails on NixOS, wrap it in an FHS environment, and confirm it runs.",
        "Add cacert and set SSL_CERT_FILE in profile so npx can fetch packages over HTTPS.",
        { text: "Read the real freebuff-nix flake and explain what each part does.", url: "https://github.com/jawuku/freebuff-nix" },
        "Compare nix develop (pure Nix PATH) with nix run on the FHS env: run which node and ls /usr in both.",
      ]},
    ],
    exercises: [],
  },

  {
    id: "modules-intro",
    title: "The Module System: Options & Config",
    group: "Module 7 — The NixOS Module System",
    summary: "The declarative configuration framework — useful even outside NixOS.",
    content: [
      { t: "p", text: "The module system is Nix's framework for declaring and merging configuration. NixOS, Home Manager, nix-darwin and countless tools are built on it, and once you learn it you can apply it to any configuration problem." },
      { t: "p", text: "A module has three parts: imports (other modules to compose), options (the typed, documented settings users can set), and config (what happens when those options are set)." },
      { t: "h", text: "Anatomy of a module" },
      { t: "read", code: "{ config, lib, pkgs, ... }:\n\nlet\n  cfg = config.services.myService;\nin\n{\n  # 1. Imports — compose other modules\n  imports = [\n    ./some-other-module.nix\n  ];\n\n  # 2. Options — declare what users can configure\n  options.services.myService = {\n    enable = lib.mkEnableOption \"my service\";\n\n    port = lib.mkOption {\n      type = lib.types.port;\n      default = 8080;\n      description = \"Port to listen on\";\n    };\n\n    package = lib.mkOption {\n      type = lib.types.package;\n      default = pkgs.myService;\n    };\n  };\n\n  # 3. Config — what gets activated when options are set\n  config = lib.mkIf cfg.enable {\n    systemd.services.myService = {\n      wantedBy = [ \"multi-user.target\" ];\n      serviceConfig.ExecStart = \"${cfg.package}/bin/my-service --port ${toString cfg.port}\";\n    };\n\n    environment.systemPackages = [ cfg.package ];\n  };\n}" },
      { t: "note", text: "The let binding at the top (cfg = config.services.myService) is the idiomatic 'declare once, use everywhere' pattern. Options you declare in options. become readable via config. — that's the loop that makes modules composable." },
    ],
    exercises: [],
  },

  {
    id: "module-options",
    title: "Option Types & Conditional Configuration",
    group: "Module 7 — The NixOS Module System",
    summary: "Typed options, mkIf / mkMerge / mkDefault, and testing modules with evalModules.",
    content: [
      { t: "h", text: "Common option types" },
      { t: "read", code: "lib.types.bool\nlib.types.int\nlib.types.str\nlib.types.path\nlib.types.package\nlib.types.port                      # 0–65535\nlib.types.enum [ \"a\" \"b\" \"c\" ]\nlib.types.listOf lib.types.str\nlib.types.attrsOf lib.types.str\nlib.types.nullOr lib.types.str\nlib.types.oneOf [ lib.types.str lib.types.int ]\nlib.types.submodule { ... }        # Nested module\nlib.types.submoduleWith { ... }    # Specializable submodule" },
      { t: "h", text: "mkIf, mkMerge, mkDefault, mkForce" },
      { t: "read", code: "# mkIf — conditional config\nconfig = lib.mkIf cfg.enable { ... };\n\n# mkMerge — combine multiple conditional blocks\nconfig = lib.mkMerge [\n  (lib.mkIf cfg.enable { systemd.services.x = ...; })\n  (lib.mkIf cfg.advanced { systemd.services.x.extraConfig = ...; })\n];\n\n# Priority system (higher numbers = lower priority):\n# mkDefault (1000) < normal (100) < mkForce (50) < mkOverride N\n# Use mkDefault for sensible defaults users can override easily" },
      { t: "h", text: "Module composition" },
      { t: "read", code: "{\n  imports = [\n    ./hardware-configuration.nix\n    ./networking.nix\n    ./services/nginx.nix\n    ./services/postgresql.nix\n    home-manager.nixosModules.home-manager\n  ];\n}" },
      { t: "h", text: "Using the module system without NixOS" },
      { t: "p", text: "lib.evalModules lets you run the module machinery for any configuration problem — and it's perfect for testing your modules without a system." },
      { t: "read", code: "{ lib, ... }:\nlet\n  eval = lib.evalModules {\n    modules = [\n      ./my-module.nix\n      { services.myService.enable = true; }\n    ];\n  };\nin\n  eval.config.services.myService.port  # => 8080 (or whatever was set)" },
      { t: "h", text: "Exercises — try on your machine" },
      { t: "list", items: [
        "Write a reusable NixOS-style module for a hypothetical web service.",
        "Use lib.evalModules to test your module without running NixOS.",
        "Create a module with submodule options (e.g. users.*.name).",
        "Use mkMerge to combine conditional configuration blocks.",
        "Study the nginx module in nixpkgs (nixos/modules/services/web-servers/nginx/).",
      ]},
    ],
    exercises: [],
  },

  {
    id: "home-manager",
    title: "Home Manager: Declarative User Environments",
    group: "Module 8 — Home Manager",
    summary: "Packages, dotfiles and programs — managed as Nix.",
    content: [
      { t: "h", text: "Installation (standalone, incl. macOS)" },
      { t: "read", code: "# flake.nix\n{\n  inputs = {\n    nixpkgs.url = \"github:NixOS/nixpkgs/nixos-unstable\";\n    home-manager = {\n      url = \"github:nix-community/home-manager\";\n      inputs.nixpkgs.follows = \"nixpkgs\";\n    };\n  };\n\n  outputs = { nixpkgs, home-manager, ... }: {\n    homeConfigurations.\"myuser\" = home-manager.lib.homeManagerConfiguration {\n      pkgs = nixpkgs.legacyPackages.x86_64-linux;\n      modules = [ ./home.nix ];\n    };\n  };\n}" },
      { t: "read", code: "home-manager switch --flake .#myuser\n\n# As a NixOS module instead:\n{ home-manager.users.myuser = import ./home.nix; }" },
      { t: "h", text: "home.nix — the basics" },
      { t: "read", code: "{ config, pkgs, lib, ... }:\n\n{\n  home.username = \"myuser\";\n  home.homeDirectory = \"/home/myuser\";\n  home.stateVersion = \"24.11\";  # Pin for reproducibility\n\n  # User packages\n  home.packages = with pkgs; [\n    ripgrep\n    fd\n    jq\n    httpie\n    bat\n    eza\n    fzf\n  ];\n\n  # Paths always on PATH (lightweight)\n  home.sessionPath = [ \"$HOME/.local/bin\" ];\n\n  # Environment variables\n  home.sessionVariables = {\n    EDITOR = \"nvim\";\n    TERMINAL = \"alacritty\";\n  };\n}" },
      { t: "h", text: "Program configuration (declarative dotfiles)" },
      { t: "read", code: "# Git\nprograms.git = {\n  enable = true;\n  userName = \"Your Name\";\n  userEmail = \"you@example.com\";\n  extraConfig = {\n    init.defaultBranch = \"main\";\n    push.autoSetupRemote = true;\n  };\n};\n\n# Shell (zsh)\nprograms.zsh = {\n  enable = true;\n  oh-my-zsh = {\n    enable = true;\n    plugins = [ \"git\" \"docker\" \"kubectl\" ];\n  };\n  initExtra = ''\n    bindkey '^R' history-incremental-search-backward\n  '';\n};\n\n# Neovim\nprograms.neovim = {\n  enable = true;\n  viAlias = true;\n  vimAlias = true;\n};\n\n# Starship prompt\nprograms.starship = {\n  enable = true;\n  settings = { add_newline = false; };\n};" },
      { t: "note", text: "The magic of Home Manager: instead of hand-editing ~/.gitconfig, ~/.zshrc and ~/.config/nvim and hoping they stay in sync, one file declares the end state and switch builds it atomically — with rollback via generations." },
    ],
    exercises: [],
  },

  {
    id: "home-manager-advanced",
    title: "Home Manager: Services, Files & the CLI",
    group: "Module 8 — Home Manager",
    summary: "User systemd services, file management, and day-to-day commands.",
    content: [
      { t: "h", text: "User-level systemd services & timers" },
      { t: "read", code: "systemd.user.services.my-backup = {\n  Unit.Description = \"My backup service\";\n  Service.ExecStart = \"${pkgs.rsync}/bin/rsync -av /source /dest\";\n  Install.WantedBy = [ \"default.target\" ];\n};\n\nsystemd.user.timers.my-backup = {\n  Unit.Description = \"Run backup hourly\";\n  Timer.OnCalendar = \"hourly\";\n  Install.WantedBy = [ \"timers.target\" ];\n};" },
      { t: "h", text: "File management (dotfiles the Nix way)" },
      { t: "read", code: "home.file = {\n  # Symlink a file from your config directory\n  \".config/alacritty/alacritty.toml\".source = ./alacritty.toml;\n\n  # Write content directly\n  \".config/git/ignore\".text = ''\n    .direnv/\n    .envrc\n    result\n  '';\n\n  # Recursive directory linking\n  \".config/nvim\".source = config.lib.file.mkOutOfStoreSymlink ./nvim;\n};" },
      { t: "h", text: "The home-manager CLI" },
      { t: "read", code: "home-manager switch               # Build and activate\nhome-manager build                # Build only\nhome-manager generations          # List generations\nhome-manager expire-generations 30d  # Clean old generations" },
      { t: "h", text: "Exercises — try on your machine" },
      { t: "list", items: [
        "Install Home Manager standalone and create a home.nix with git, zsh and Starship.",
        "Configure Neovim entirely through Home Manager.",
        "Create a user systemd timer service that runs a script daily.",
        "Migrate 3 existing dotfiles into home.file (or program modules).",
        "Set up Home Manager as a NixOS module (if you run NixOS).",
      ]},
    ],
    exercises: [],
  },

  {
    id: "ci-caching",
    title: "Nix in CI: GitHub Actions & Binary Caches",
    group: "Module 9 — CI/CD, Caching & Nix in Production",
    summary: "Continuous integration and sharing prebuilt artifacts.",
    content: [
      { t: "h", text: "GitHub Actions for Nix" },
      { t: "read", code: "# .github/workflows/ci.yml\nname: CI\non: [push, pull_request]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: DeterminateSystems/nix-installer-action@main\n      - uses: DeterminateSystems/magic-nix-cache-action@main\n\n      - run: nix flake check\n      - run: nix build\n      - run: nix develop --command echo \"Dev shell works\"" },
      { t: "p", text: "The Determinate Systems actions handle installing Nix and caching evaluation results. The magic-nix-cache-action shares the nix store across CI runs, which makes flake check / build dramatically faster on repeat runs." },
      { t: "h", text: "Binary caching with Cachix" },
      { t: "p", text: "A binary cache stores prebuilt artifacts so other machines (and CI) can download them instead of building. Cachix makes this one command." },
      { t: "read", code: "nix profile install nixpkgs#cachix\ncachix authtoken <token>\ncachix use my-cache\nnix build | cachix push my-cache\n\n# In CI (GitHub Actions):\n# - uses: cachix/cachix-action@v14\n#   with:\n#     name: my-cache\n#     authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'" },
      { t: "h", text: "Auto-updating flake inputs" },
      { t: "read", code: "# .github/workflows/update-flake.yml\nname: Update Flake\non:\n  schedule:\n    - cron: '0 0 * * 0'  # Weekly\n  workflow_dispatch:\n\njobs:\n  update:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: DeterminateSystems/nix-installer-action@main\n      - uses: DeterminateSystems/update-flake-lock@main\n        with:\n          pr-title: \"chore: update flake.lock\"" },
      { t: "note", text: "This 'bot opens a PR when nixpkgs moves' pattern is the modern answer to channels: instead of silently drifting, every update is a reviewable, revertible pull request." },
    ],
    exercises: [],
  },

  {
    id: "docker-copy",
    title: "Docker Images, nix copy & Static Binaries",
    group: "Module 9 — CI/CD, Caching & Nix in Production",
    summary: "Shipping Nix-built artifacts everywhere.",
    content: [
      { t: "h", text: "Nix in Docker" },
      { t: "p", text: "dockerTools builds minimal, reproducible container images — no Dockerfile needed. Layered images share content-addressable layers with other images built from the same nixpkgs." },
      { t: "read", code: "{ pkgs, dockerTools, myApp }:\n\ndockerTools.buildLayeredImage {\n  name = \"my-app\";\n  tag = \"latest\";\n  contents = [ myApp pkgs.cacert ];\n  config = {\n    Cmd = [ \"${myApp}/bin/my-app\" ];\n    ExposedPorts = { \"8080/tcp\" = {}; };\n  };\n}" },
      { t: "read", code: "nix build .#docker\ndocker load < result\ndocker run my-app" },
      { t: "h", text: "nix copy & remote stores" },
      { t: "read", code: "# Copy a closure to another machine\nnix copy --to ssh://user@host /nix/store/...-my-package\n\n# Copy to a binary cache\nnix copy --to s3://my-bucket?region=us-east-1 ./result\n\n# Copy between stores\nnix copy --from https://cache.nixos.org /nix/store/...-hello" },
      { t: "h", text: "Statically linked binaries" },
      { t: "read", code: "# For fully static Go/Rust binaries (great for Docker FROM scratch):\npkgs.pkgsStatic.callPackage ./my-app.nix {}\n\n# Or with mkDerivation:\nstdenv.mkDerivation {\n  NIX_CFLAGS_COMPILE = \"-static\";\n  configureFlags = [ \"--enable-static\" \"--disable-shared\" ];\n}" },
      { t: "h", text: "Exercises — try on your machine" },
      { t: "list", items: [
        "Set up a GitHub Actions workflow that runs nix flake check on every push.",
        "Create a Cachix cache and push builds to it.",
        "Build a Docker image from a Nix package using dockerTools.",
        "Write a flake that cross-compiles a Go binary for x86_64-linux and aarch64-linux.",
        "Set up the update-flake-lock action to automatically PR flake updates.",
      ]},
    ],
    exercises: [],
  },

  {
    id: "capstone",
    title: "Capstone Projects",
    group: "Module 10 — Capstone Projects",
    summary: "Five real-world projects that tie the whole course together.",
    content: [
      { t: "p", text: "Everything you've learned, applied. Pick one or two of these — each is designed to force you through the full loop: write Nix, build it, cache it, deploy it." },
      { t: "h", text: "Project 1 — Personal development environment flake" },
      { t: "list", items: [
        "devShells for Python, Node.js, Rust, and a 'full' shell composed with inputsFrom.",
        "A formatter (nixpkgs-fmt, alejandra, or nixfmt-rfc-style).",
        "packages for any custom tools you've built.",
        "direnv integration so the right shell activates per directory.",
        "A companion home.nix or NixOS module installing your preferred tools.",
      ]},
      { t: "h", text: "Project 2 — Package something from source" },
      { t: "list", items: [
        "Choose a small open-source tool not yet in nixpkgs.",
        "Package it with stdenv.mkDerivation (or the language-specific builder).",
        "Add patches if needed.",
        "Optional stretch goal: contribute it to nixpkgs!",
      ]},
      { t: "h", text: "Project 3 — NixOS or Home Manager configuration" },
      { t: "list", items: [
        "Refactor your configuration into modules.",
        "Use flakes to pin everything.",
        "Set up CI to test that your configuration builds.",
        "Share it as a public dotfiles repo.",
      ]},
      { t: "h", text: "Project 4 — Containerized application with Nix" },
      { t: "list", items: [
        "Build a web service (any language).",
        "Package it with Nix.",
        "Create a minimal Docker image with dockerTools.",
        "Set up CI that builds, caches and publishes the image.",
      ]},
      { t: "h", text: "Project 5 — Nix library or overlay" },
      { t: "list", items: [
        "Create a flake that provides a useful overlay or library.",
        "Example: a set of lib functions, or an overlay that patches common packages.",
        "Publish it on GitHub so others can use it as a flake input.",
      ]},
      { t: "note", text: "Whichever you choose, aim to hit every layer: derivation → flake → cache → CI → deployment. That loop is Nix 'in production', and completing it once beats reading about it ten times." },
    ],
    exercises: [],
  },

  {
    id: "appendix",
    title: "Legacy & Deprecated Features",
    group: "Reference",
    summary: "What you'll see in old code — and why you shouldn't use it.",
    content: [
      { t: "note", text: "These are included for historical awareness and reading legacy code only. Do NOT use them in new projects." },
      { t: "h", text: "nix-env — imperative package management" },
      { t: "read", code: "nix-env -iA nixpkgs.hello    # Install (MUTATES user profile)\nnix-env -e hello             # Remove\nnix-env -q                   # List" },
      { t: "p", text: "Why deprecated: imperative, non-reproducible, no lockfiles. Use nix profile (imperative but flake-compatible), nix shell (temporary), or declarative config instead." },
      { t: "h", text: "nix-channel — mutable channels" },
      { t: "read", code: "nix-channel --add https://nixos.org/channels/nixpkgs-unstable nixpkgs\nnix-channel --update" },
      { t: "p", text: "Why deprecated: channels are mutable — nix-channel --update gives you a different nixpkgs revision depending on when you run it. Flakes and flake.lock solve this." },
      { t: "h", text: "<nixpkgs> angle-bracket paths" },
      { t: "read", code: "import <nixpkgs> {}" },
      { t: "p", text: "Why deprecated: relies on the $NIX_PATH environment variable, which varies across machines. Use explicit inputs in flakes instead." },
      { t: "h", text: "nix-shell shebang" },
      { t: "read", code: "#! /usr/bin/env nix-shell\n#! nix-shell -i bash -p python3" },
      { t: "p", text: "Status: still works — and it's actually one of the few remaining reasonable uses of nix-shell, since a script's interpreter is resolved before Nix could run. For new scripts, prefer nix run or a flake-based approach." },
      { t: "h", text: "Unpinned fetchGit / fetchTarball" },
      { t: "read", code: "builtins.fetchGit { url = \"...\"; }   # No rev/sha — impure!" },
      { t: "p", text: "Why deprecated: without a hash or revision, this fetches the latest commit, breaking reproducibility." },
      { t: "h", text: "builtins.currentSystem" },
      { t: "read", code: "if builtins.currentSystem == \"x86_64-linux\" then ... else ..." },
      { t: "p", text: "Why avoided in flakes: flakes require explicit system parameters for cross-compilation support. Use the system argument from flake outputs instead." },
    ],
    exercises: [],
  },

  {
    id: "glossary",
    title: "Glossary of Nix Terminology",
    group: "Reference",
    summary: "The vocabulary of Nix, A to Z. Terms in bold are defined elsewhere in this glossary.",
    content: [
      { t: "h", text: "A" },
      { t: "list", items: [
        "attribute — a key-value pair inside an attribute set. E.g. name = \"hello\";",
        "attribute set — the primary composite data structure: a collection of name = value pairs like a dictionary or JSON object: { x = 1; y = 2; }.",
      ]},
      { t: "h", text: "B" },
      { t: "list", items: [
        "binary cache — a remote store serving prebuilt substitutes (.nar archives) for store paths. Examples: cache.nixos.org, Cachix, FlakeHub Cache.",
        "bubblewrap — a Linux sandboxing tool (binary bwrap) using user namespaces to build isolated filesystems without root; buildFHSEnvBubblewrap invokes it at runtime to mount the synthetic FHS.",
        "builder — the program executed inside the sandbox to perform the build; usually bash via stdenv, but any executable works.",
        "buildInputs — runtime dependencies of a derivation: libraries the built package links against or needs at runtime.",
        "builtins — functions built into the Nix interpreter itself, accessed via the builtins. prefix.",
      ]},
      { t: "h", text: "C" },
      { t: "list", items: [
        "callPackage — the nixpkgs function that reads a package function's argument names and auto-injects matching packages from pkgs.",
        "channel — a mutable URL pointer to a nixpkgs branch (e.g. nixpkgs-unstable). Deprecated in favour of flakes.",
        "closure — the complete transitive set of store paths a given path depends on. Inspect with nix path-info -r ./result.",
        "content-addressed — store paths named from the hash of their output contents (vs input-addressed, from the inputs).",
        "cross-compilation — building on one platform for a different target platform, via pkgsCross.",
      ]},
      { t: "h", text: "D" },
      { t: "list", items: [
        "default.nix — the canonical package entry point; nix-build looks for it by default.",
        "derivation (.drv file) — the low-level build recipe in /nix/store specifying the builder, args, env vars and inputs.",
        "devShell — a flake output (devShells.<system>.<name>) defining a development environment; entered with nix develop.",
        "direnv — a shell extension that auto-loads environments per directory; with nix-direnv it auto-activates Nix dev shells.",
      ]},
      { t: "h", text: "E–F" },
      { t: "list", items: [
        "evaluation — computing a Nix expression's value without building anything; Nix is lazily evaluated.",
        "experimental features — unstable-but-common features (flakes, nix-command) that must be enabled explicitly.",
        "fetcher — a function that downloads source and stores it with a content-verified hash: fetchurl, fetchFromGitHub, fetchgit, fetchTarball.",
        "fixed-output derivation (FOD) — a derivation with a pre-declared output hash; FODs are allowed network access (this is how fetchers work).",
        "flake — a self-contained, standardised Nix project with declared inputs (pinned in flake.lock) and standardised outputs.",
        "flake.lock — the auto-generated lockfile pinning exact git revisions and nar hashes of all inputs; commit it.",
        "flake-utils — the popular helper (github:numtide/flake-utils) for system-agnostic outputs via eachDefaultSystem.",
        "follows — a flake directive making an input reuse another input's revision (e.g. inputs.nixpkgs.follows = \"nixpkgs\";).",
      ]},
      { t: "h", text: "G–I" },
      { t: "list", items: [
        "garbage collection (GC) — deleting store paths unreachable from any GC root; nix store gc.",
        "generation — a numbered, immutable snapshot of a profile; the basis of instant rollback.",
        "hermetic build — a build isolated from the host: no network, no undeclared files, no ambient env vars.",
        "Home Manager — a Nix tool for declaratively managing user environments (packages, dotfiles, services).",
        "hook — a shell fragment running at a point in the build lifecycle (preBuild, postInstall, …).",
        "impure — depending on state outside declared inputs; flakes default to pure evaluation, opt out with --impure.",
        "inherit — the keyword that copies attributes: inherit x y; means x = x; y = y;.",
        "input-addressed — store paths hashed from the derivation's inputs rather than its outputs.",
      ]},
      { t: "h", text: "L–M" },
      { t: "list", items: [
        "lazy evaluation — expressions are only computed when their value is needed.",
        "lib — the nixpkgs standard library of pure Nix helpers for lists, strings, attrsets, option types.",
        "mkDerivation — stdenv.mkDerivation: the primary package builder, wrapping builtins.derivation with stdenv and phases.",
        "mkShell / mkShellNoCC — builders for shell environments (with/without a C toolchain).",
        "module — a function declaring options and config, composed via lib.evalModules.",
        "mkOption — declares a typed configuration option.",
        "mkEnableOption — shorthand for a boolean enable option with a standard description.",
      ]},
      { t: "h", text: "N–O" },
      { t: "list", items: [
        "nar (Nix ARchive) — the deterministic archive format used inside the store and served by binary caches.",
        "nativeBuildInputs — build-time dependencies (compilers, build tools) needed during the build itself.",
        "nix-darwin — NixOS-style declarative system configuration for macOS.",
        "nixpkgs — the primary package collection: packages, stdenv, lib, modules, and more.",
        "Nix store — /nix/store: where every built package, source and .drv lives, at immutable, hashed paths.",
        "NixOS — the Linux distribution built on Nix; entire systems configured declaratively.",
        "overlay — a function (final: prev: { … }) creating a modified view of nixpkgs.",
        "override / overrideAttrs — overriding a package's function arguments / derivation attributes.",
      ]},
      { t: "h", text: "P–R" },
      { t: "list", items: [
        "profile — a directory of symlinks into the store; each update creates a new generation.",
        "pure evaluation — evaluation forbidden from the host filesystem, network and env vars (flake default).",
        "realisation — executing a derivation's builder to produce its output.",
        "reproducible build — bit-for-bit identical output across equivalent environments.",
      ]},
      { t: "h", text: "S" },
      { t: "list", items: [
        "sandbox — the isolated environment builders run in: no network, restricted filesystem, controlled env vars.",
        "setup hook — a script shipped with a package, sourced automatically in dependents' builds (e.g. pkg-config's PKG_CONFIG_PATH hook).",
        "shell.nix — the classic convention for a dev shell with mkShell; entered via nix-shell.",
        "stdenv — the standard environment: toolchain plus the standard configure → make → make install lifecycle.",
        "store path — a fully-qualified path inside the Nix store: /nix/store/<hash>-<name>-<version>.",
        "substitute — a prebuilt store path downloaded from a binary cache instead of being built.",
        "symlink farm — a directory tree of symlinks pointing into the Nix store, forming a synthetic /usr (e.g. /usr/bin/git → /nix/store/…/bin/git); a buildFHSEnv output is a wrapper script plus a symlink farm, which bwrap mounts as a real /usr at runtime.",
      ]},
      { t: "h", text: "Common acronyms" },
      { t: "table", head: ["Acronym", "Meaning"], rows: [
        ["CA", "Content-Addressed (store paths based on output hash)"],
        ["FHS", "Filesystem Hierarchy Standard (the Linux /usr, /lib, /etc layout)"],
        ["FOD", "Fixed-Output Derivation"],
        ["GC", "Garbage Collection"],
        ["NAR", "Nix ARchive"],
        ["NUR", "Nix User Repository (community package overlays, like the AUR)"],
      ]},
    ],
    exercises: [],
  },

  {
    id: "commands",
    title: "Command Cheat Sheet",
    group: "Reference",
    summary: "The traditional and flake commands side by side.",
    content: [
      { t: "p", text: "The single most useful page to come back to: every everyday task, in both the classic and flake idioms." },
      { t: "table", head: ["Task", "Traditional", "Flake"], rows: [
        ["Build package", "nix-build", "nix build"],
        ["Build specific", "nix-build -A foo", "nix build .#foo"],
        ["Enter dev shell", "nix-shell", "nix develop"],
        ["Run app", "nix-shell -p pkg --run cmd", "nix run .#app"],
        ["Search packages", "nix search nixpkgs query", "nix search nixpkgs query"],
        ["Update deps", "nix-channel --update", "nix flake update"],
        ["Garbage collect", "nix-collect-garbage", "nix store gc"],
        ["Show deps", "nix-store -q --tree ./result", "nix-store -q --tree ./result"],
      ]},
      { t: "note", text: "The flake column is the default for new work; the traditional column exists because you will absolutely encounter it in real-world repos and tutorials." },
    ],
    exercises: [],
  },

  {
    id: "resources",
    title: "Resources & Further Reading",
    group: "Reference",
    summary: "The best places to go deeper, once the course is done.",
    content: [
      { t: "h", text: "Official" },
      { t: "list", items: [
        { text: "nix.dev — the official learning resource.", url: "https://nix.dev" },
        { text: "NixOS Manual — NixOS reference.", url: "https://nixos.org/manual/nixos/stable/" },
        { text: "Nixpkgs Manual — packaging reference (incl. the lib functions).", url: "https://nixos.org/manual/nixpkgs/stable/" },
        { text: "Nix Reference Manual — the language and CLI reference.", url: "https://nix.dev/manual/nix/latest/" },
      ]},
      { t: "h", text: "Community" },
      { t: "list", items: [
        { text: "NixOS Discourse — the community forum.", url: "https://discourse.nixos.org/" },
        { text: "NixOS Wiki — community knowledge base.", url: "https://wiki.nixos.org/" },
        { text: "Nix Pills — deep dive into Nix internals.", url: "https://nixos.org/guides/nix-pills/" },
        { text: "Zero to Nix — quick-start guide, flake-focused.", url: "https://zero-to-nix.com/" },
        { text: "Determinate Systems Blog — modern Nix practices.", url: "https://determinate.systems/posts/" },
      ]},
      { t: "h", text: "Tools" },
      { t: "list", items: [
        { text: "direnv + nix-direnv — auto-loading Nix environments.", url: "https://github.com/nix-community/nix-direnv" },
        { text: "Cachix — binary cache hosting.", url: "https://www.cachix.org/" },
        { text: "devenv — higher-level dev environment abstraction on top of Nix.", url: "https://devenv.sh/" },
        { text: "nixpkgs-fmt / Alejandra — Nix formatters.", url: "https://github.com/nix-community/nixpkgs-fmt" },
        { text: "nil / nixd — Nix language servers for your editor.", url: "https://github.com/oxalica/nil" },
      ]},
      { t: "h", text: "Example configurations to study" },
      { t: "list", items: [
        { text: "nixpkgs — the package repository itself.", url: "https://github.com/NixOS/nixpkgs" },
        { text: "nixos-hardware — hardware-specific NixOS modules.", url: "https://github.com/NixOS/nixos-hardware" },
        { text: "home-manager — user environment management.", url: "https://github.com/nix-community/home-manager" },
        { text: "nix-darwin — Nix on macOS.", url: "https://github.com/LnL7/nix-darwin" },
        { text: "Community dotfiles — search GitHub for flake.nix + home-manager / nixos.", url: "https://github.com/search?q=flake.nix+home-manager&type=repositories" },
      ]},
    ],
    exercises: [],
  },
];
